[project]
name = "linux-cis-hardener"
version = "0.1.0"
description = "Enterprise-grade Linux security auditing and automated remediation suite based on CIS Benchmark Level 1"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "cibi-dev", email = "cibi-dev@users.noreply.github.com" }
]
dependencies = [
    "pydantic>=2.0,<3.0",
    "pyyaml>=6.0,<7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9.0",
    "pytest-cov>=5.0,<6.0",
    "bandit>=1.7,<2.0",
    "mypy>=1.10,<2.0",
]

[project.scripts]
cis-hardener = "cis.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cis"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
warn_unused_ignores = false
warn_return_any = false
disallow_untyped_defs = false
check_untyped_defs = true
exclude = [
    '^tests/',
    '^benchmarks/',
    '^examples/',
    '^\.venv/',
]
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --cov=src/cis --cov-report=term-missing"
pythonpath = ["src"]

[tool.coverage.run]
source = ["src/cis"]
omit = ["tests/*", "benchmarks/*"]

[tool.coverage.report]
fail_under = 90
show_missing = true
